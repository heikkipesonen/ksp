
import { Rectangle } from "pixi.js"
import { elementIntersectsWithLine } from "../support"

describe('Support', () => {
  describe('intersection', () => {
    it('', () => {

      expect(elementIntersectsWithLine(new PIXI.Graphics())(new PIXI.Graphics())).toEqual(true)
    })
  })

})
